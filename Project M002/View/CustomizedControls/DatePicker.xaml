<UserControl x:Class="MyToDos.View.CustomizedControls.DatePicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyToDos.View.CustomizedControls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="700">
    <Grid Background="{DynamicResource Color_Background}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <RadioButton GroupName="ModeBar"></RadioButton>
        <RadioButton GroupName="ModeBar"></RadioButton>
        <local:DateComboBox Visibility="Collapsed" x:Name="ComboBox" Width="320" Grid.Column="1" Grid.RowSpan="2"></local:DateComboBox>
        <local:Calendar x:Name="Calendar" Grid.Column="1" Grid.RowSpan="2"></local:Calendar>
    </Grid>
</UserControl>
